# 🏭 PLC Control (IT/OT)
The AI never drives actuators directly — a **PLC** executes physical sorting, keeping compliance with **IEC 61508** & **ISO 13849**. Communication via **Modbus TCP / OPC-UA / Profinet**. An incremental rotary **encoder** synchronizes detection with air jets / pneumatic gates (<5 ms actuation).
