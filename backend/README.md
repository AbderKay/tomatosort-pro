# 🔌 Backend (FastAPI)
Async REST + WebSocket backend for production data, alerts, and classification results.

- `api/` — RESTful & WebSocket endpoints.
- `services/` — business logic (ingestion, alerting, aggregation).
- `models/` — data schemas.
