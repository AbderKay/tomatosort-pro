# 🔁 MLOps
**MLflow** tracks every training run (metrics, params, artifacts) with versioning and a central registry. An **Active Learning** loop flags low-confidence cases for targeted human annotation, then models are retrained on Gold-layer data and auto-deployed after validation.
