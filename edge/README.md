# ⚡ Edge Inference
Model inference on the industrial PC (IPC, fanless). Optimization via **OpenVINO** (x86/VPU) and **TensorRT** (NVIDIA GPU); optional **TPU** accelerators. Target: >60 FPS, <20 ms latency via quantization & pruning.
